/**
 * Public KiCad Footprint Environment kernel.
 *
 * Consumers should use the runtime/model boundary exported here. The internal
 * S-expression file reader is intentionally not the architectural boundary.
 */
export * from "./kicad/footprint";
